<p style="color: green"><%= notice %></p>

<h1>Tasks</h1>

<div id="tasks" class="d-flex p-2">
  <% @tasks.each do |task| %>
    <%= render task %>
  <% end %>
</div>

<%= link_to "New task", new_task_path, class:"btn btn-info"%>
