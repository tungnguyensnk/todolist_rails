<p style="color: green"><%= notice %></p>

<%= link_to "Back to groups", groups_path %>
<%= render @group %>

<div>
  <%= link_to "Edit this group", edit_group_path(@group), class:"btn btn-info" %>

  <%= button_to "Destroy this group", @group, method: :delete, class:"btn btn-danger" %>
</div>
